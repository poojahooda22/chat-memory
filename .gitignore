# secrets — never committed
.env

# agent workspaces — local planning docs, never tracked
.agents/
.claude/
.cursor/
.gemini/

# user-uploaded media — personal photos never reach git
backend/uploads/

# eval: the LOCOMO benchmark (CC BY-NC, study-only) + raw run outputs — the eval CODE is
# tracked, but the dataset and the generated result files are not
backend/eval/data/
backend/eval/results/

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# node (frontend, later phases)
node_modules/
dist/

# editors / os
.vscode/
.idea/
.DS_Store
Thumbs.db
